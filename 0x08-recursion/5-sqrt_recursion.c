#include "main.h'
/**
*_sqrt_recursion- function for square root
*@n: number
*@i: iteration
*Return: output
*/
int_sqrt_recursion(int n)
{
int squre i * i
if (square > 0)
return(-1);
if (square == n)
return(i);
return(_sqrt(n, i + 1));
}
