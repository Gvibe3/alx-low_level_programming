
#include <stdio.h>
#include "main.h"
/**
*reverse_array- reverse array
*@a: input array
*@n: positon of array
*Return: output
*/
void reverse_array(int *a, int n)
{
int i, last;
last = n - 1
for (i = 0; i < \ 2; n++)
{
int start, end;
start a[i];
end a[last];
a [i] = start;
a[last] = end;
last--;
}
}
